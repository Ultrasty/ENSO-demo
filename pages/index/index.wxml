<!-- 预测页面 -->
<view wx:if="{{value=='predict'}}">

    <!-- 这个 --td-navbar-padding-top:0 非得要写在内联样式里才生效，不能放到index.scss里去，无语了 -->
    <t-navbar style="--td-navbar-padding-top:0;--td-navbar-right:0px;" title="ENSO集合预测结果查询" t-class-title="nav-title" />

    <!-- popup的图表 -->
    <!-- 这popup的duration属性不生效啊，无语 -->
    <t-popup visible="{{visible}}" bind:visible-change="onVisibleChange" placement="center">
        <view class="block">
            <t-icon t-class="close-btn" name="close-circle" size="32" color="grey" bind:tap="onClose" />
            <view>
                <view class="chartRotated">
                    <ec-canvas id="chartRotated" canvas-id="chartRotated" ec="{{ ecRotated }}"></ec-canvas>
                </view>
            </view>
        </view>
    </t-popup>

    <view class="subtitle">起报时间：</view>

    <!-- 日期选择器 -->
    <t-cell title="选择日期" hover note="{{monthText}}" arrow data-mode="month" bindtap="showPicker" t-class="pannel-item" />

    <!-- 年月 -->
    <t-date-time-picker title="选择日期" visible="{{monthVisible}}" mode="month" value="{{month}}" format="YYYY-MM" bindchange="onConfirm" bindpick="onColumnChange" bindcancel="hidePicker" start="{{start}}" end="{{end}}" />

    <view class="subtitle">预测结果：</view>
    <view>
        <view bind:tap="handlePopup" class="chartMain">
            <ec-canvas id="chartMain" canvas-id="chartMain" ec="{{ ecMain }}"></ec-canvas>
        </view>
    </view>

    <view class="subtitle">结果描述：</view>

    <view bind:tap="test" style="border: 8rpx solid rgb(200,200,200);margin: 20rpx;padding: 40rpx 20rpx 40rpx 20rpx;">在未来6个月，冷异常逐渐减弱，拉尼娜现象逐渐消亡，之后逐渐转入暖异常</view>

    <!-- 底部留空，防止被标签栏遮住 -->
    <view style="height: 180rpx;"></view>
</view>


<!-- 分析页面 -->
<view wx:if="{{value=='analyse'}}">

    <t-navbar style="--td-navbar-padding-top:0;--td-navbar-right:0px;" title="不同起报月份的ENSO预测分析" t-class-title="nav-title" />

    <view class="wrapper">
        <t-collapse value="{{activeValues}}" bind:change="handleChange">
            <t-collapse-panel header="预报结果逐月对比" value="{{0}}" expandIcon>
                此处可自定义内容此处可自定义内容此处可自定义内容此处可自定义内容此处可自定义内容此处可自定义内容此处可自定义内容此处可自定义内容
            </t-collapse-panel>
        </t-collapse>
        <t-collapse value="{{activeValues}}" bind:change="handleChange">
            <t-collapse-panel header="预报结果误差柱状图" value="{{1}}" expandIcon>
                此处可自定义内容此处可自定义内容此处可自定义内容此处可自定义内容此处可自定义内容此处可自定义内容此处可自定义内容此处可自定义内容
            </t-collapse-panel>
        </t-collapse>
        <t-collapse value="{{activeValues}}" bind:change="handleChange">
            <t-collapse-panel header="误差分析箱型图" value="{{2}}" expandIcon>
                此处可自定义内容此处可自定义内容此处可自定义内容此处可自定义内容此处可自定义内容此处可自定义内容此处可自定义内容此处可自定义内容
            </t-collapse-panel>
        </t-collapse>
        <t-collapse value="{{activeValues}}" bind:change="handleChange">
            <t-collapse-panel header="预报结果逐月相关性分析" value="{{3}}" expandIcon>
                此处可自定义内容此处可自定义内容此处可自定义内容此处可自定义内容此处可自定义内容此处可自定义内容此处可自定义内容此处可自定义内容
            </t-collapse-panel>
        </t-collapse>
    </view>
</view>

<!-- 底部标签栏 tab bar -->
<t-tab-bar value="{{value}}" bindchange="onChange" theme="tag" split="{{false}}">
    <t-tab-bar-item wx:for="{{list}}" wx:key="index" value="{{item.value}}"> {{item.label}} </t-tab-bar-item>
</t-tab-bar>